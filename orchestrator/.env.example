# Orchestrator's own Algorand wallet — this is what actually pays the workers.
# From generate-wallets.js. Use the private key / mnemonic, NOT the address, here.
ORCHESTRATOR_PRIVATE_KEY=

PROVENANCE_AGENT_URL=http://localhost:4021
TRUST_SYNTHESIS_AGENT_URL=http://localhost:4022
VERIFICATION_AGENT_URL=http://localhost:4023

# Only needed once you wire in real LLM planning/synthesis (Phase 5)
LLM_API_KEY=
