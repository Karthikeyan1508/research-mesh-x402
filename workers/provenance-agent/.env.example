# Address that receives payment for this agent's endpoint (from generate-wallets.js)
PROVENANCE_AGENT_ALGO_ADDRESS=

# GoPlausible facilitator (testnet). Confirm current URL at https://facilitator.goplausible.xyz/
FACILITATOR_URL=https://facilitator.goplausible.xyz

# Port this service listens on
PORT=4021

# Real search API key (Tavily/SerpAPI) — only needed once you swap out mockSearch()
SEARCH_API_KEY=
